{
  "name": "api-factory",
  "version": "1.0.0",
  "private": true,
  "description": "NestJS multi-tenant microservices library foundation monorepo",
  "workspaces": [
    "libs/*",
    "apps/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "test": "npm run test --workspaces --if-present"
  },
  "devDependencies": {
    "@nestjs/cli": "^10.0.0",
    "@nestjs/common": "^10.0.0",
    "@nestjs/core": "^10.0.0",
    "@nestjs/testing": "^10.0.0",
    "@types/node": "^20.11.0",
    "rxjs": "^7.8.1",
    "typescript": "^5.3.3"
  }
}
