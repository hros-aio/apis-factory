{
  "name": "api-factory",
  "version": "1.0.0",
  "private": true,
  "description": "NestJS multi-tenant microservices library foundation monorepo",
  "workspaces": [
    "libs/*",
    "apps/*"
  ],
  "scripts": {
    "clean": "rm -rf dist libs/*/dist tsconfig.tsbuildinfo libs/*/tsconfig.tsbuildinfo",
    "prebuild": "npm run clean",
    "build": "npm run build -w @new-hros/libs-core && npm run build -w @new-hros/libs-sql && npm run build -w @new-hros/libs-mongo && npm run build -w @new-hros/libs-apis",
    "test": "npm run test --workspaces --if-present"
  },
  "devDependencies": {
    "@nestjs/cli": "^10.0.0",
    "@nestjs/common": "^10.0.0",
    "@nestjs/core": "^10.0.0",
    "@nestjs/testing": "^10.0.0",
    "@types/node": "^20.11.0",
    "@types/jest": "^29.5.11",
    "rxjs": "^7.8.1",
    "typescript": "^5.3.3"
  }
}